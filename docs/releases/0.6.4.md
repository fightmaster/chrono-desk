# Chrono Desk 0.6.4

Task: CHR-SW-025
Docs-Impact: CROSS_PROJECT

Accepts the Stopwatch `unassign_number` operation: clears the bib and issued
flag while preserving participant identity, profile and race. The number is
available for reassignment. Journal, retries and feed replication retain the
existing transaction and conflict rules. Timing evidence blocks ordinary
clearing. Existing commands remain accepted; no database migration is needed.

Install this receiver before using «Убрать номер» in Stopwatch v1.114.
The shared fixture checksum is pinned in service tests; a receiver/feed test
verifies profile preservation, retry and replication to a second Desk.
