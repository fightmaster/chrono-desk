# Chrono Desk 0.6.5

Task: CHR-SW-026
Docs-Impact: CROSS_PROJECT

Accepts `create_registration` with an empty bib and issuePacket=false from
Stopwatch v1.115. New participants retain stable IDs and race assignments and
can receive numbers later. Empty numbers do not collide with other unnumbered
participants. Existing numbered creation and assignment checks remain intact.
No migration. Install this receiver before using unnumbered creation in PWA.

The shared fixture is checksum-pinned. Tests cover persistence alongside an
existing unnumbered registration, retry and feed replication to a second Desk.
