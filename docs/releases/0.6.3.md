# Chrono Desk 0.6.3

Task: CHR-SW-021
Docs-Impact: OPERATIONAL

Replacement for the unpublished 0.6.2 binary release. The 0.6.2 tag remains
immutable: its workflow stopped because a release metadata test still required
VERSION 0.6.1. The test now validates semantic version syntax; existing tests
still require matching native bundle and application versions.

Includes the [0.6.2 packet issuance changes](0.6.2.md). Runtime code is unchanged
from that candidate. RUN5 3ddf17b and Stopwatch v1.112 are already deployed.
